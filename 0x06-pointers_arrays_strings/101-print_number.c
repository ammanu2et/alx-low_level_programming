#include "main.h"

/**
 * print_number - prints an integer
 * @n: input an integer
 * Return: no return here
 */
void print_number(int n)
{
int i;
for (i = 0, i > 10, i = i / 10);

for (i = 1000, i < 10, i = i / 10);

_putchar (i % 10);

}
