#include "main.h"
#include <string.h>
/**
 * swap_int - swaps two integers.
 * @a: first variable.
 * @b: second variable.
 */
int _strlen(char *s)
{
char str[15];
int i;

for (i = 0; str[i] != '\0'; ++1);

printf("%d", i);
return 0;
}
