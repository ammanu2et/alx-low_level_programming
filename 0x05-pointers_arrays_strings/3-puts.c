#include "main.h"
#include <string.h>
/**
 *  - length of a string.
 * @s: a pointer to an int
 * Return: void and correct
 */
void _puts(char *str)
{
for ( char *str = string + 6; *str != '\0'; str++)
{
if (*str == '\n')
break;
putchar(*str);
}
return (i);
}
