#include "main.h"
#include <string.h>
/**
 *  - length of a string.
 * @s: a pointer to an int
 * Return: void and correct
 */
void _puts(char *str)
{
while (*str = getchar(), *str >= 0)
putchar(*str);
}
