#include "main.h"
/**
 *main = main.h function
 *_islower = function returns alphabet c
 *int c = the call function
 *return 1 if its true
 *return 0 not
 */
int _islower(int c)

/*_islower = function returns alphabet c
 *int c = the call function
 */
{
if ((c >= 97 && c <= 122))
return (1);
else
return (0);
}
