#include "main.h"
/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int main(void)
{
int n, i;
for (i = 0; i <= 15; ++i)
{

    printf("%d * %d = %d\n", n, i, n * i);
    printf('\n');
}
    return (0);
}
